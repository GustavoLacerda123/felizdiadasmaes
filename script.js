// Efeito de digitação na mensagem (opcional)
document.addEventListener('DOMContentLoaded', function() {
    const message = document.querySelector('.message');
    message.style.opacity = '1'; // Suave aparecimento
});
